# 编译指导（build guide）

代码改完且用户授权后按本文件编译。**仅支持下方各平台列出的常用模块**；未列出的模块不做自动编译，标注「未编译验证、需人工/CI 复核」。

> **当前不支持自动编译**：agent 无就地构建环境，无法触发 soong `m` / lunch 编译。本文件暂时仅作为编译命令备忘，供用户在具备构建环境的机器上手动执行；待具备自动编译条件（agent 可调用构建环境）后再接自动编译。

> 触发时机：编译耗时长且不可逆，须用户明确授权后才跑；未授权 → 同上标注。

## SA8295

环境准备：<待填，如 source build/envsetup.sh && lunch <target>>

| 模块 (侧) | 编译命令 | 产物位置 |
|----------|---------|---------|
| <待填，如 audio HAL (GVM)> | <待填，如 m vendor.audio-hal> | <待填> |
| <待填，如 csd2 (QNX)> | <待填> | <待填> |

## MT8091

环境准备：<待填>

| 模块 | 编译命令 | 产物位置 |
|------|---------|---------|
| <待填> | <待填> | <待填> |

## SA8397

环境准备：<待填>

| 模块 | 编译命令 | 产物位置 |
|------|---------|---------|
| <待填> | <待填> | <待填> |
